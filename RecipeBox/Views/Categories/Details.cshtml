@{
    Layout = "_Layout";
}

@model RecipeBox.Models.Category;
@using RecipeBox.Models;
@using System.Collections.Generic;

<h2>Category Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>

@if (@Model.JoinEntities.Count == 0)
{
    <p>This category does not contain recipes</p>
}
else
{
    <h4>Items the category contains:</h4>
    <ul>
    @foreach (RecipeCategory join in Model.JoinEntities)
        {
            <li>@join.Recipe.Description</li>
        }
    </ul>
}

<p>@Html.ActionLink("Back to categories", "Index")</p>
<p>@Html.ActionLink("Edit Category", "Edit", new { id = Model.CategoryId })</p>
<p>@Html.ActionLink("Delete Category", "Delete", new { id = Model.CategoryId })</p>
<p>@Html.ActionLink("Add new recipe", "Create", "Recipes")</p>


@* string recipes = "";
@if (recipe.JoinEntities.Count == 0)
{
recipes = "This item does not have any tags";
}
else
{
List<string> recipeList = new List<string>();
@foreach (RecipeCategory join in recipe.JoinEntities)
{
recipeList.Add(join.Recipe.Description);
}
recipes = String.Join(", ", recipeList);
}
<li>@recipe.Description | Recipes: @recipes</li> *@